export default function FanAppPage() {
  return (
    <section aria-labelledby="fan-app-title" data-page-shell="fan-app">
      <h1 id="fan-app-title">Fan App</h1>
      <p>Estructura base de página.</p>
    </section>
  );
}
